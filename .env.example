# No environment variables are required for the prototype.
# All state is in-memory React state; there is no backend, database, or API key.
# Add Vite-prefixed variables here when wiring up real services
# (e.g. VITE_API_BASE_URL=https://api.example.com).
